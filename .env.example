# Copy to .env (gitignored) and fill in. Never commit real keys.
OPENAI_API_KEY=
# Optional: model id; default is the newest gpt-*mini* model visible to the key.
OPENAI_MODEL=
# Optional kill switch: 0 = always use template explanations.
COPILOT_ENABLED=1
