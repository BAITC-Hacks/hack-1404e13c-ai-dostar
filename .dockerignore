# Secrets and local state never go into the image
.env
.env.*
!.env.example
data/

# Environments, caches, VCS
.git
.venv
**/__pycache__
**/*.pyc
.pytest_cache
.ipynb_checkpoints
.mem0
.codex

# Not needed at runtime: CSV copies (the app reads the Excel files), docs, notebooks, tests
datasets/*_CSV
docs
notebooks
tests
